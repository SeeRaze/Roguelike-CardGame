# Дизайн прокачки карт = универсальный движок кат.4 (компаунд `k^f`)

> Спецификация ПЕРЕД кодом (Шаг 0). Источник правды для сим-итерации и живой
> игры. Парадигма — [[balance-curve-framework]]: враг растёт `E₀·g^f`, игрок
> аддитивно; потолок пробивают компаундящие движки кат.4, которых дефицит
> ([[scaling-audit]]). Эта система закрывает дефицит РАЗОМ для всех классов.
>
> Статус: ОБСУЖДЕНО И УТВЕРЖДЕНО с юзером. НЕ реализовано. Порядок работ — §8.

---

## 1. Зачем (место в математике)

Сила карты ≈ `base · (линейка уровней) · Π(множители тегов)`.

- **Линейка уровней (+1 за уровень)** — слой СТЕНЫ (кат.1, линейна, ОСОЗНАННО
  проигрывает `g^f`). Делает раннюю карту чуть толще, не ломает кривую.
- **Теги в майлстоунах (5/10/15)** — слой ПОТОЛКА (кат.4, компаунд).

Почему майлстоуны = `k^f` (доминирующий источник экспоненты игрока):
```
уровень ≈ c·f         (c = скорость ковки с костра — ГЛАВНАЯ ручка)
#майлстоунов ≈ уровень/5
сила ×mult ≈ k^(#майлстоунов) = k^(c·f/5) = (k^(c/5))^f
```
Условие пробития потолка из framework: `Σlog k > log(g/p)`. Конкретные ручки —
размер `k` тега, скорость `c`, шаг майлстоунов, рост врага `g`. Все собраны в §6.

### КЛЮЧЕВОЙ инсайт: условие САМО разделяет стену и потолок
Тег — это `k × P(условие выполнено)`. В **wall-метрике** (бот не строит билд)
`P→0` → эффективный множитель ≈1 → **стена остаётся честной**. В **ceiling**
(билд заточен под условие) `P→1` → полный `k` → **потолок пробивается**.
⇒ Условный компаунд самоограничен в стене → СОХРАНЯЕТ зазор стена↔потолок
(всё пространство геймплея). Безусловный ×k надувал бы и стену → гонка
вооружений с врагом → зазор схлопывается. Поэтому теги > чистые мультипликаторы.

Это поглощает план [[wall-ceiling-taxonomy-plan]]: не нужно вручную клеить
статусам ярлык wall/ceiling — условный тег ПО ПОСТРОЕНИЮ ceiling-инструмент,
а до выполнения условия лежит как wall-флат.

---

## 2. Архитектура паспортов карт (решение юзера: тег на ЭКЗЕМПЛЯРЕ)

Тег живёт на КОНКРЕТНОЙ карте (этот «Удар» прокачан до 15 с ×mult «из пустой
руки»; другой «Удар» в той же колоде — голый). Это даёт «экспоненту
реиграбельности»: комбинация разных грейдов одной карты в одной колоде.

Чтобы НЕ раздувать объект карты и сохранить плоскую (sim-friendly) архитектуру —
**разделяем сущности**:

- **Объект `Card`** несёт только идентичность: `card_id` (тип, напр. `"strike"`)
  + `uid` (уникальный инстанс в этом забеге). Логика розыгрыша — без изменений.
- **Вся мета-прокачка** — в ЕДИНОМ плоском словаре игрока
  `player.deck_forge_state: dict[uid → ForgeRecord]`.

```
ForgeRecord = {
    "level": int,                 # 0..∞ (линейка +1)
    "slots": [TagInstance, ...],  # открытые майлстоунами слоты
}
TagInstance = { "tag_id": str, "kind": "add"|"mult", "value": float }
```

Почему так:
- **Sim:** headless-расчёт = быстрый поиск по словарю (`deck_forge_state[uid]`),
  не нужно таскать состояние внутри боевого объекта.
- **UI:** карта просто обрастает СПИСКОМ условий в описании (чистый рендер,
  растёт вниз). Бейдж уровня = `level`.
- **Персистентность:** `deck_forge_state` живёт весь забег, НЕ сбрасывается
  `reset_combat_statuses` (как `persistent_allies`). Реюз стартует на новом забеге.

### Врезка в существующий код
- `Card.__init__`: добавить `card_id` + `uid` (генерится при создании инстанса;
  в симе — детерминированный счётчик, БЕЗ `Math.random`/`uuid`-недетерминизма).
- ВНИМАНИЕ: сейчас апгрейд = `self.name += "+"` + bool `self.upgraded`. Старый
  одноступенчатый upgrade МИГРИРУЕТ в `level≥1` (level 1 = текущий `+`). Старые
  карты с `upgraded=True` читаются как `level=1` (обратная совместимость).
- Канал «какая карта играется» — переиспользовать `_card_being_played`
  (CombatManager:141/157, уже есть для Потока). Tag-движок берёт `uid` оттуда.

---

## 3. Экономика ковки (источник очка `c` + босс-капы)

### Базовый источник — КОСТЁР (ручка `c`)
Костёр (`ui/Campfire.py`, этаж `FLOORS_PER_ACT-2` опц. с магазином) получает
4-е действие **«Ковка»**: потратить ресурс → +1 уровень выбранной карты.
Скудость ресурса = стратегия «что качать» (концентрировать в 2-3 карты =
острый билд, размазать = универсал). Экономика = РЕГУЛЯТОР гонки (framework §6).

- **Ресурс ковки — отдельная валюта `FP` (Forge Points), УТВЕРЖДЕНО.** НЕ золото:
  чище экономика (ковка не конкурирует с магазином/реликвиями за один кошелёк),
  и `FP` лимитируется ЭТАЖАМИ → прямой контроль скорости `c` (= наклона игрока)
  независимо от золотого дохода. `player.forge_points`. Капля FP за бой/этаж
  (точный дроп — калибровка), плюс возможный бонус с босса.
- **Стоимость уровня — РАСТУЩАЯ ВНУТРИ ТИРА, УТВЕРЖДЕНО.** Цена +1 уровня растёт
  с текущим уровнем карты в пределах тира (напр. `FP_cost = base + level·step`),
  сбрасывает «крутизну» на новом тире после босс-гейта. Эффект: тактический
  прессинг — добить карту до майлстоуна дорого → выбор «дожать одну до ×mult или
  поднять две до +mult». Естественный анти-degenerate тормоз концентрации.
- Накопление в БОЮ — ВЫРЕЗАНО из базового движка (защита сима от петель
  «играй→качай→играй» и ранних рантануть). Уходит в пул РЕДКИХ артефактов позже.

### ✅ ЗАКАЛКА (Отдых) — альтернативный сток FP в Max HP (УТВЕРЖДЕНО, Сессия 39+)
На костре игрок выбирает: тратить FP на **ковку карт** (глубина урона/защиты)
ИЛИ на **Закалку** — навсегда **+15% к ТЕКУЩЕМУ `max_hp`** (`TEMPER_HP_PCT=0.15`,
компаунд-процент, конвенция §10.3) **+ полное исцеление**. Цена — `TEMPER_FP_COST`
(гипотеза = 15 FP = один тир ковки; калибруется).
**Зачем:** даёт Max-HP-ФУНДАМЕНТ, на который ложатся экспоненциальные оборонные
теги (HealEffect капается `max_hp` — без роста бака хил упирается в потолок; щит
поглощает за ход, но ваншот пробивает HP напрямую). Закалка = линейно-компаундный
слой выживаемости (растит `p` базово), теги = экспоненциальный (растят `p` круто).
Вместе закрывают оборонный дефицит ([[balance-findings-forge-flip]]) с двух сторон.
**Решение бота (sim, §8 порядок):** ПРОАКТИВНО, «по гонке кривых» (калибр. 39.3):
бот закаляется на КАЖДОМ костре, пока ДАВЛЕНИЕ БОЯ следующего акта
(`урон_за_ход · INCOMING_FIGHT_TURNS`) ≥ `TEMPER_PROACTIVE_RATIO · max_hp`, иначе
кует. Калибровано: `RATIO=0.6`, `HP_PCT=0.20`, `FP_COST=10`. Закалка стала истинным
per-floor рычагом (раньше — только аварийной при ваншоте). Живой `Campfire.py` —
3-я/4-я кнопка выбора, раскатка в 39.5.

### ✅ ТРИЕДИНСТВО ЭКОНОМИКИ (УТВЕРЖДЕНО + КАЛИБРОВАНО, С39.3)
Закалка, %-Ивенты и Артефакты — единый неразрывный движок выживаемости (закрывает
оборонный дефицит [[balance-findings-forge-flip]]). **Бои=база · %-Ивенты=скачки ·
Артефакты=глобальные масштабираторы.** Сим-слой: `forge.py` (Закалка) + `events.py`
(EventPolicy) + `economy.py` (золото); свип — `managers/balance/sweep.py`.

**Результат калибровки против `g=1.026`:** база (Бои+Ивенты) ФЛИПАЕТ HP-зависимые
классы в акт 3 (Маг med 86 · Призыв. 82 · Воин 64 · Берсерк 33 + дикий свинг),
НЕ двигает Разбойника/Друида (горлышко = DPS/оборона, лечится атак.тегами 39.4).

**%-СОБЫТИЯ — открытый раздел акт-скейла (Вариант 3 юзера, БЕЗ капов):**
азартный размен ресурса ради рычага, диапазон % жёстко привязан к Акту —
- Акт 1 (эт.1–20): микро-сделки **5–15%** от стейта.
- Акт 2 (эт.21–40): средний темп **15–30%**.
- Акт 3+ (эт.41+): ва-банк **30–50%** (сжигание половины золота/HP ради рычага).

Каденция ДЕТЕРМИНИРОВАНА (как живой MapGenerator): `EVENTS_PER_ACT=2` ноды на акт
на фикс. этажах; стохастика (`EVENT_WIN_CHANCE=0.5`) живёт ВНУТРИ узла → хардкор-
свинг с дисперсией, но воспроизводимо при фикс. seed. Ставка = золото (широта
билда), с акта 3 — HP («ва-банк»). Выигрыш → компаунд-% к `max_hp` ИЛИ пачка FP
(`EVENT_REWARD_MULT=2.0`, УМЕРЕННАЯ точка — зазор под артефакты).

**Артефакты — placeholder-заглушки (нейтральны):** `artifact_fp_mult`,
`artifact_max_hp_add` (`forge.py`), `artifact_gold_mult` (`economy.py`). Сим-
математика УЖЕ учитывает будущий лейт-раскорм реликвиями (запас +15-20%): артефакты
= поздний катализатор эт.85-100 (база НАМЕРЕННО не закрывает эт.100 в одиночку,
иначе реликвии обесценятся). Точные значения — после реализации кода артефактов.

### Контрольные точки — БОСС-ГЕЙТЫ (снимают КАП уровня, не дают очков)
Босс открывает следующий ярус прокачки: до него уровень карты заперт капом.
Это вторая, НЕЗАВИСИМАЯ ручка стены: даже при быстрой ковке (высокий `c`) игрок
не превратит уровни в `×mult`, пока не убил нужного босса. ⇒ кусочно-экспонента
игрока: медленный старт (только `+mult` разрешён) → разблокировка `×mult` после
босса. Контроль взрыва чисел ВСТРОЕН в прогрессию, а не только в формулу.

**Бонус:** это и есть [[boss-filter-ladder]] (боссы-фильтры, к которым сим был
слеп) — но встроенный в прогрессию, которую сим БУДЕТ моделировать. Босс
перестаёт быть невидимой стеной, становится видимой контрольной точкой компаунда.

### ✅ Развязка двух шкал (УТВЕРЖДЕНО)
Две РАЗНЫЕ оси, увязаны жёстко:
- **Уровни КАРТЫ** с майлстоунами: **5 / 10 / 15** (шкала прокачки).
- **Босс-ЭТАЖИ** в коде: каждые **20** (`FLOORS_PER_ACT`) → 20/40/60/80/100.

| Босс-этаж | Снимает кап уровня карты до | Открывает |
|---|---|---|
| 20 | **5** | слот-1 (ранний тег, `+mult`) |
| 40 | **10** | слот-2 (средний тег, `+mult`) |
| 60 | **15** | слот-3 (**легендарный**, `×mult`) |
| 80 | **20** | 1-й круг Гипер-зарядов (§4-bis) |
| 100 | **25** | 2-й круг Гипер-зарядов |

⇒ `×mult` физически недостижим раньше этажа ~60 → **железная защита ранней
стены** (сим знает потолок компаунда на каждом этаже). С босса-80 слоты больше
НЕ открываются (заперты на 3) — банк FP идёт в Гипер-заряд существующих тегов
(§4-bis). Числовые значения капов — финально калибруются в симе, но СТРУКТУРА
(босс↔майлстоун) зафиксирована.

### ✅ Эконом-фиксация (УТВЕРЖДЕНО, Сессия 39+)
Подтверждённая боевая модель экономики FP (зафиксирована после диагностики 39.3):
- **Приток FP:** 1/2/3 FP за бой в Актах 1/2/3+ (`FORGE_POINTS_PER_ACT`, cap 3),
  ~120 FP за забег. Накопление В БОЮ вырезано (защита сима от петель).
- **Стоимость со сбросом по тирам:** тиры 0-5 / 5-10 / 10-15, каждый = 15 FP
  (`base+(level mod s)·step`). Идеальная мега-карта 15-го уровня = ровно **45 FP**.
  Избыток FP после 15-го этажа идёт на покупку Гипер-зарядов (§4-bis).
- **Каденция — ПОСТ-БОСС ковка (УТВЕРЖДЕНО):** игрок получает интерфейс
  распределения FP СРАЗУ после боссов 20/40/60, уничтожая мёртвую зону в 19
  этажей (раньше ковка была только на костре, банк лежал). В симе — вызов
  `forge.forge_between_acts` сразу после `on_boss_defeated` в раннере.

### ✅ Эконом-мост (УТВЕРЖДЕНО): золото=широта, FP=глубина
Две валюты не конкурируют, но связаны мостом:
- **Scrap (Переплавка):** уничтожение покупных пред-прокачанных карт на костре
  возвращает **100% вложенных FP** в банк (`invested_fp(level)` уже есть как
  основа). Позволяет переразложить глубину между картами без потери ресурса.
- **Дроссель Магазина:** пред-прокачанные карты в Магазине дорожают
  ЭКСПОНЕНЦИАЛЬНО от уровня, ассортимент узкий (1-2 позиции). Золото покупает
  широту (новые карты/реликвии), но не подменяет FP-глубину. Принято как
  проектный стандарт.

---

## 4. Слоты и теги (мягкий старт → истинный компаунд)

Майлстоун уровня открывает СЛОТ под тег. Теги расслоены по силе:

| Майлстоун (уровень карты) | Слот | Класс тега | Эффект |
|---|---|---|---|
| 5  | слот 1 | ранний | `+mult` (аддитивный, мягкий) |
| 10 | слот 2 | средний | `+mult` (аддитивный) |
| 15 | слот 3 | **легендарный** | **`×mult`** (мультипликативный, ИСТИННЫЙ компаунд) |
| 20/25/… | — (слотов ВСЕГДА 3) | Гипер-заряд (§4-bis) | `×OVERCHARGE_STEP^grade` к существующему тегу |

- Несколько тегов на ОДНОЙ карте стакаются → произведение при выполнении
  нескольких условий (максимум билд-комбинаторики).
- **Получение тега — ДРАФТ ИЗ 3, УТВЕРЖДЕНО.** При открытии слота игрок выбирает
  1 из 3 случайных тегов, соответствующих ТЕКУЩЕМУ тиру майлстоуна (слот-3 → 3
  легендарных `×mult`-варианта, не ранние). Реиграбельность + осмысленный выбор
  билда. Пул-кандидатов фильтруется Smart Weighting (§10.1) — родственные
  текущему билду теги всплывают чаще. Для сима: драфт = детерминированный выбор
  ceiling-ботом лучшего из 3 под архетип (см. §8); wall — случайный/без ковки.
- **Инвариант для сима:** `×mult`-тег ФИЗИЧЕСКИ не существует до уровня 15, а
  уровень 15 недостижим до босса-60 (§3). ⇒ сим ЗНАЕТ: компаунд `×mult` не
  появляется раньше этажа ~60 → ранняя стена математически защищена.
- UI: карта показывает список «[уровень] эффект · теги: …». Чистый рендер.

---

## 4-bis. Гипер-заряд (Overcharge) — бесконечная экспонента БЕЗ раздувания UI

**Решение юзера (Сессия 39+).** Закон Пост-15: реализует бесконечный рост `k^f`,
но защищает UI и кодовую базу от раздувания числом слотов.

### Закон
1. **Число слотов на карте НАВСЕГДА = 3** (открываются на 5/10/15). UI чист:
   карта несёт максимум 3 строки тегов, сколько бы ни качалась дальше.
2. С **уровня 20** и каждые следующие **+5** (20/25/30/35…) костёр/босс-ковка
   НЕ создают новый слот, а предлагают **Гипер-заряд** одного из УЖЕ открытых
   тегов карты.
3. При Гипер-заряде растёт `grade` тега (0 → 1 → 2 …). Внутренняя сила тега
   (его `+a` или `×k`, атакующего ИЛИ оборонного) умножается на прогрессивный
   коэффициент: **`base · OVERCHARGE_STEP^grade`** (гипотеза `OVERCHARGE_STEP=1.2`).
   Текст условия НЕ меняется — растёт только базовая цифра множителя.

### Математика (почему это чистая бесконечная экспонента)
`grade ≈ (level−15)/5`, а `level ≈ c·f` ⇒ `grade ≈ c·f/5`. Сила тега
`= k · OVERCHARGE_STEP^grade = k · (OVERCHARGE_STEP^(c/5))^f` — экспонента по
этажу, основание `OVERCHARGE_STEP^(c/5)`. Слоты больше не растут (UI заперт), но
ВНУТРИ трёх слотов сила уходит в небо (Balatro big-nums). Это переносит ручку
бесконечности с «числа слотов» на «grade существующего тега» — ровно то, что
UI/код переживают без раздувания.

### Где (точки врезки)
- `TagInstance` получает `grade: int` (дефолт 0). `forge_output_multiplier`
  масштабирует вклад тега на `OVERCHARGE_STEP^grade`.
- `ForgePolicy`: при ковке уровня ≥20, кратного `MILESTONE_STEP`, вместо
  `slots.append` инкрементирует `grade` самого ценного открытого тега
  (концентрация: гипер-заряжаем тег, дающий максимум при текущем снимке).
- Босс-капы расширяются под Overcharge: босс-80 → кап ур.20, босс-100 → ур.25
  (каждый открывает новый круг Гипер-зарядов).

---

## 5. Реестр предикатов-условий (стартовый пул)

Data-driven, как `DetonationRegistry`: запись = `{tag_id, predicate(ctx), kind,
value, channel, label}`. Новое условие = 1 запись + предикат. `ctx` = срез боя в
момент удара/действия (рука, поле, цель, счётчики хода).

### КАНАЛ тега (`channel`) — универсальная ковка (Развилка №1, Вариант 1)
**Решение юзера (Сессия 39+):** система ковки ОФИЦИАЛЬНО универсальна. Тег
действует не только на урон, но и на ЗАЩИТУ/СУСТЕЙН. Каждый тег несёт поле
`channel ∈ {damage, shield, heal}`:
- `damage` — множитель к итоговому урону (`calculate_damage`, шаг 7).
- `shield` — множитель к генерации Щита/Барьера (`ShieldEffect`/`BarrierEffect`).
- `heal`  — множитель к исцелению/регенерации (`HealEffect`/`RegenEffect`).

**Почему это закрывает оборонный дефицит ([[balance-findings-forge-flip]]):** в
симе щит поглощает урон 1:1 и сбрасывается за ход → ×mult на щит масштабирует
поглощение за ход (рычаг против `E₀·g^f`). Барьер Воина НЕсгораем (`shield =
carry + barrier` каждый ход) → ×mult на барьер **компаундит ход-за-ходом** =
истинная экспонента защиты, симметричная атаке. Только экспоненциальный щит
выдержит экспоненциальный урон поздних этажей. `Σlog k > log(g/p)` — теперь `p`
(выживаемость) ТОЖЕ растёт экспонентой, а не только числитель.
**Хил слабее по построению** (капается `max_hp`, не пробьёт ваншот) → это
сустейн/идентичность, основной вес флипа кладём на щит/барьер.

### Generic (нейтральные, в общем пуле)
| tag_id | Условие (предикат) | channel | Резонанс с классом |
|---|---|---|---|
| `empty_hand` | рука пуста на момент розыгрыша | damage | темпо/Разбойник |
| `first_card` | 1-я карта за ход | damage | контроль, заряд-карты |
| `low_hp` | HP игрока < порога | damage | Берсерк (HP-как-ресурс) |
| `fortified` | в руке только карты защиты | shield | универсальный танк |

### Классовые атакующие (резонанс с движком кат.4)
| tag_id | Условие | channel | Класс |
|---|---|---|---|
| `per_minion` | за каждого живого союзника | damage | **Призыватель** (Свора N²) |
| `per_poison` | у цели есть Яд (масштаб от стака) | damage | **Друид** (Вирулентность) |
| `per_shield` | у игрока есть щит/Барьер | damage | **Воин** (Барьер = атака) |
| `per_bleed` | за стак Кровотечения на цели | damage | **Разбойник** (Кровожадность) |
| `missing_hp` | за % недостающего HP | damage | **Берсерк** (Ярость крови) |
| `per_combo` | за накопленное Мастерство | damage | **Маг** (Мастерство) |

### Классовые ОБОРОННЫЕ/СУСТЕЙН (Вариант 1 — закрывают `p`)
| tag_id | Условие | channel | Класс / смысл |
|---|---|---|---|
| `bulwark` | в руке только карты защиты | shield | ×щит, если рука «оборонная» (танк) |
| `per_barrier` | за стак Барьера у игрока | shield | **Воин** — барьер растит сам себя (компаунд) |
| `venomous_ward` | за стак Яда/Вирулентности на цели | heal | **Друид** — сустейн от яда (хил→яд→хил) |
| `last_stand` | HP игрока < порога | shield | **Берсерк** — щит на грани (контр-ваншот) |

Резонанс намеренный: одна система прокачки выражается по-разному на каждом
классе → идентичность бесплатно. Атакующие теги дают «числа до небес», оборонные
— «выживаемость до небес». Класс собирает СВОЙ профиль (Друид может качать и
яд-урон, и яд-сустейн → две оси одной фантазии).

---

## 6. Формулы-ручки (предмет калибровки в симе)

Все числа НИЖЕ — стартовые гипотезы, КАЛИБРУЮТСЯ симом против `g`. Реальные —
после §8. Заносятся в `_balance_knobs.md` (раздел «Прокачка») после калибровки.

| Ручка | Симв. | Гипотеза | Смысл / влияние |
|---|---|---|---|
| Скорость ковки | `c` | ~1 уровень / костёр | наклон экспоненты игрока (костров ~1/акт) |
| Бонус линейки/уровень | `δ` | +1 к base-эффекту | слой стены (кат.1) |
| Ранний тег | `+a` | +0.5 mult | мягкий старт (аддитивно к множителю) |
| Легендарный тег | `×k` | ×1.5 при условии | истинный компаунд (кат.4) |
| Шаг майлстоунов | `s` | 5 уровней | реже майлстоун = медленнее `k^f` |
| Кап до босса-N | — | 5/10/15 | ступень разблокировки (§3) |
| Рост врага (есть) | `g` | HP 1.028 / DMG 1.026 | EnemySpawner (для сравнения наклонов) |

Условие баланса (из framework): подобрать `c, k, s` так, чтобы СОБРАННЫЙ билд
(ceiling) флипал наклон в плюс (`(k^(c/s)) > g`), а НЕсобранный (wall) — нет
(`P(условие)→0` держит множитель ≈1). Зазор = доказательство двойной экспоненты.

---

## 7. Точки врезки в код (карта реализации)

| Что | Где | Действие |
|---|---|---|
| Паспорт карты | `core/cards/base.py` `Card.__init__` | +`card_id`, +`uid`; миграция `upgraded`→`level` |
| Словарь ковки | `core/players/base.py` | +`self.deck_forge_state={}`; НЕ в `_COMBAT_RESET_KEYS` |
| Чтение тегов | `core/EffectCalculator.calculate_damage` | новый шаг (после реликвий): прочесть теги карты из `_card_being_played.uid`, применить add/mult по предикатам |
| Канал «какая карта» | `managers/CombatManager` `_card_being_played` | УЖЕ ЕСТЬ (Поток) — переиспользовать |
| Реестр предикатов | `core/ForgeRegistry.py` (НОВЫЙ) | data-driven, как DetonationRegistry |
| Ковка на костре | `ui/Campfire.py` | 4-е действие «Ковка» (+уровень за FP, растущая цена) |
| Валюта FP | `core/players/base.py` | +`self.forge_points`; дроп в runner/RewardManager |
| Босс-кап | хук `on_boss_defeated` (есть, persistent.py) | поднять `forge_level_cap` (20→5,40→10,60→15) |
| Драфт тегов | слот-открытие | 1 из 3 текущего тира, фильтр Smart Weighting (§10.1) |
| Smart Weighting | драфт тегов/наград | вес = base·(1+affinity), реюз `_deck_themes` |
| Trigger Cap | `managers/CombatManager` | счётчик глубины, `MAX_TRIGGER_DEPTH=5` (§10.2) |
| Процент-конвенция | все ивенты/обмены | % не флат (§10.3); прецедент `REST_HEAL_PCT` |
| Снимок состояния | `CombatManager` `_play_snapshot` | §10.6: контекст на момент намерения; предикаты читают его |
| Резолв паспорта | `resolve_forge_record(card, player)` | §10.4 temp uid → родитель; §10.7 null-safe |
| Тир награды | `reward_level_for_floor(floor)` | §10.5: драфт/магазин пред-прокачаны по акту |
| Big-numbers | UI урона (позже) | формат 1.2K/3.4M — отдельная итерация |

---

## 8. Порядок работ (УТВЕРЖДЁН — «сначала сим видит условия»)

Урок слепоты сима ([[balance-findings-shock-dilution]], Кровожадность): движок,
который сим не пилотирует, НЕЛЬЗЯ откалибровать. Поэтому:

1. **Сим: модель FP + уровней карт** — `deck_forge_state` + `forge_points` в
   sim-игроке, ковка на костре в runner (ceiling-бот концентрирует FP в ядро
   билда по растущей цене; wall — не качает или размазывает). Босс-капы уровня.
   **Trigger Cap (§10.2) закладывается СРАЗУ** — счётчик глубины в bot/runner
   (защита headless-прогона от зависания с первого шага).
2. **Сим: предикаты в damage-calc** — ceiling-бот ВЫПОЛНЯЕТ условия (играет
   так, чтобы `empty_hand`/`per_minion`/… срабатывали); wall — нет. Иначе тег
   невидим (грабли Шока). Драфт тегов из 3 + **Smart Weighting (§10.1)** —
   ceiling-бот берёт лучший под архетип, affinity-вес моделируется.
3. **Калибровка** — свип `c, k, s, affinity` против `g`: flip наклона у ceiling,
   плоская стена у wall. **Процент-конвенция (§10.3)** даёт масштаб-инвариантные
   формулы → проще свип. Зелёный регресс-гард (`baseline.py`).
4. **Чистка слепых зон классов** — каждый класс-предикат реально пилотируется
   ботом (резонанс §5 измерим).
5. **ТОЛЬКО ПОСЛЕ зелёного теста** — раскатка в живой код: `Card.uid`,
   `ForgeRegistry`, ковка+FP в `Campfire.py`, чтение тегов в боёвке, драфт-UI,
   UI-список, босс-капы, Trigger Cap в реал-боёвке. + тесты + headless + доки.
6. Big-numbers UI — отдельная поздняя итерация (Balatro-формат). Процент-скейл
   (§10.3) — сквозная конвенция, внедряется по ходу (не отдельный этап).

---

## 9. Развилки (ВСЕ РЕШЕНЫ — юзер, ревью дока)

1. **Шкалы карта↔босс** — ✅ УВЯЗАНЫ ЖЁСТКО: босс-20→кап ур.5, босс-40→кап ур.10,
   босс-60→кап ур.15 (§3, таблица). Железная защита ранней стены.
2. **Ресурс ковки** — ✅ отдельная валюта **`FP` (Forge Points)**, лимит этажами
   (§3). Чистая экономика + прямой контроль `c`.
3. **Стоимость уровня** — ✅ **РАСТУЩАЯ внутри тира** (`base+level·step`, сброс
   крутизны на новом тире). Тактический прессинг, анти-degenerate (§3).
4. **Получение тегов** — ✅ **драфт из 3** вариантов текущего тира (§4), пул под
   Smart Weighting (§10.1).
5. **Оборонный дефицит (Развилка №1, Сессия 39+)** — ✅ **Вариант 1**: ковка
   УНИВЕРСАЛЬНА, теги несут `channel` (damage/shield/heal), оборонные/сустейн
   ×mult растят `p` (§5). Закрывает [[balance-findings-forge-flip]]: атакующий
   тег-движок НЕ универсален (оборонные классы упирались в ту же стену) → дали
   экспоненту и защите.
6. **Бесконечный рост без раздувания UI (Сессия 39+)** — ✅ **Гипер-заряд**
   (§4-bis): слотов всегда 3, с ур.20 каждые +5 растёт `grade` существующего
   тега `×OVERCHARGE_STEP^grade`. Бесконечная экспонента живёт в `grade`, не в
   числе слотов.

---

## 10. Глобальные гард-рейлы под экспоненту пула (УТВЕРЖДЕНО)

Три предохранителя — закладываются В АРХИТЕКТУРУ СИМА с самого начала (§8), не
прикручиваются потом. Защищают пул и числа от вырождения при росте контента.

### 10.1 Smart Weighting (анти-размытие пула)
**Проблема:** растущий пул тегов/артефактов → драфт из 3 даёт случайный мусор не
по билду (Pool Dilution) → игрок не может СОБРАТЬ архетип → ceiling не пробивает.
**Решение:** шанс выпадения тега/артефакта повышается, если он РОДСТВЕНЕН уже
выбранным в билде (общий `tag_id`-семейство / класс-резонанс §5 / тема колоды).
Переиспользует `_deck_themes`/`_card_score` из `managers/balance/builds.py`
(там уже есть бонус за тему). Реализация: вес кандидата = `base · (1 + affinity)`,
`affinity` = совпадение с текущими тегами/архетипом.
**Для сима:** ceiling-бот и так берёт лучшее под архетип — Smart Weighting
делает это ДОСТУПНЫМ (нужные теги реально выпадают), сокращая разрыв «теория vs
что выпало». Ручка: сила `affinity` (0 = чистый рандом = размытие, ∞ = всегда
по билду = нет разнообразия). Калибруется.

### 10.2 Trigger Cap (анти-бесконечный цикл)
**Проблема:** цепочки «Эха» (ретриггер) + детонации могут рекурсить
(удар→эхо→детонация→ещё удар→…) → бесконечный цикл в реал-тайме + переполнение
чисел. Уже частично снято в детонациях (RAW `take_damage` вне `calculate_damage`,
см. трекер D1), но Эхо + теги добавят новые пути рекурсии.
**Решение:** ЖЁСТКИЙ глобальный счётчик вложенных триггеров на одно действие —
**макс 5** (`MAX_TRIGGER_DEPTH`). Достигнут → цепочка обрывается, лог. Считает
ВСЕ ретриггеры/детонации/каскады тегов суммарно за один розыгрыш карты.
**Где:** счётчик в `CombatManager` (зеркало в bot/runner), инкремент на входе
в каждый триггер-хендлер (Эхо/детонация/каскад тега), сброс по завершении
розыгрыша (рядом с `_card_being_played = None`). Ручка: `MAX_TRIGGER_DEPTH=5`
→ `_balance_knobs.md`.
**Для сима:** защищает headless-прогон от зависания + делает урон ОГРАНИЧЕННЫМ
сверху за действие (предсказуемость замера).

### 10.3 Процентный скейл (анти-инфляция плоских чисел)
**Проблема:** при big-numbers (урон в тысячах к эт.100) плоские ивенты/обмены
(«−10 HP», «+25 золота») становятся неощутимыми → мёртвый контент.
**Решение:** ВСЕ ивенты/обмены/жертвы переводятся с плоских чисел на ПРОЦЕНТЫ
от текущего ресурса (% HP / % золота / scale-by-floor). Прецедент уже есть:
костёр лечит `REST_HEAL_PCT=0.30` от недостающего HP (Сессия 38), `lose_hp`
сквозь щит. Распространяем конвенцию: Ритуал крови, Проклятый сундук,
карты-жертвы, цены — всё в %.
**Для сима:** проценты МАСШТАБ-ИНВАРИАНТНЫ → одна формула работает на эт.5 и
эт.100, не нужно ре-тюнить плоские пороги под каждый тир. Упрощает калибровку.
**Конвенция под рост (§ из [[wall-ceiling-taxonomy-plan]]):** новый ивент
ОБЯЗАН быть в %, не во флате. Аналог регресс-гарда — правило при добавлении.

---

## 10-bis. Рантайм-предохранители ядра (реал-тайм + групповой движок)

Четыре правила корректности под динамику боя. Закладываются в 39.2 (логика
предикатов) и переезжают в живой код в 39.5. Без них предикаты врут или ронят
движок при копиях карт, поздних наградах, микрофрейм-гонках и гибели целей.

### 10.4 Динамическое наследование (временные карты)
**Проблема:** копии/«призрачные» карты, созданные ВНУТРИ боя (механики
копирования, боевые ивенты), не имеют записи в постоянном `deck_forge_state` →
их теги/уровень теряются.
**Требование:** копия, созданная динамически в бою, генерирует ВРЕМЕННЫЙ uid по
маске `{parent_uid}_temp_{id}` и ЗЕРКАЛИТ уровень+теги паспорта родителя. По
окончании боя ВСЕ временные паспорта принудительно стираются.
**Где:** хелпер `resolve_forge_record(card, player)` — если uid временный,
читает запись родителя; очистка временных uid рядом с концом боя (зеркало
`reset_combat_statuses`). Сим: тот же хелпер в чтении тегов damage-calc.

### 10.5 Закон минимального тира наград (защита позднего драфта)
**Проблема:** карты 0-го уровня из наград/Магазина на поздних этажах = «мусор»
против экспоненты врага → душат колоду.
**Требование:** уровень и число встроенных тегов карт в наградах/Магазине
ДИНАМИЧЕСКИ скейлятся по текущему акту/босс-чекпойнту. С Акта 2 игрок получает
жизнеспособный драфт (напр. базовый уровень 5 для этажей 21+).
**Где:** `reward_level_for_floor(floor)` (зеркало босс-капов §3) — драфт/магазин
создают карту уже прокачанной до этого уровня (+ соответств. теги). Сим:
greedy_draft/default_draft под forge пред-прокачивают добор. Ручка в §11.

### 10.6 Снимок состояния (State Snapshot для предикатов)
**Проблема:** в динамике (и с прицелом на реал-тайм) рука/энергия/статусы могут
измениться за микрофрейм между намерением (клик) и фактическим расчётом урона
(анимации) → предикаты выдают ложный False.
**Требование:** в момент фиксации намерения разыграть карту движок делает
ИЗОЛИРОВАННЫЙ снимок контекста (рука, ресурсы, поля, счётчики хода). Расчёт
локального компаунда тегов в damage-calc идёт СТРОГО по снимку, что бы ни
случилось со столом миллисекундой позже.
**Где:** `CombatManager` пишет `_play_snapshot` рядом с `_card_being_played`
(на входе в розыгрыш), чистит на выходе. Предикаты читают ТОЛЬКО снимок, не
живое состояние. Бонус: снимок замораживает цель → автоматически закрывает 10.7.

### 10.7 Безопасность позиционных предикатов (null-safe, групповой движок)
**Проблема:** предикаты, читающие геометрию стола (миньоны/конкретные враги),
могут уронить математику в None или вызвать софтлок, если цель гибнет внутри
каскада триггеров до конца расчёта карты.
**Требование:** ВСЕ позиционные/геометрические проверки — null-safe. Цель
уничтожена в каскаде → предикат безопасно закрывается по дефолту (False/0), без
падения движка.
**Где:** каждый предикат в `ForgeRegistry` обёрнут защитой от пустых/мёртвых
слотов; снимок (10.6) уже даёт замороженные значения → двойная страховка.

---

## 11. Резюме ручек для `_balance_knobs.md` (после калибровки)

| Ручка | Символ | Где будет |
|---|---|---|
| Скорость ковки (дроп FP/этаж) | `c` | экономика костра/runner |
| Стоимость уровня (`base+lvl·step`) | — | `ui/Campfire.py` + зеркало sim |
| Бонус линейки/уровень | `δ` | `Card`/ForgeRegistry |
| Ранний/средний тег | `+a` | ForgeRegistry |
| Легендарный тег | `×k` | ForgeRegistry |
| Шаг майлстоунов | `s=5` | ForgeRegistry |
| Босс-капы уровня | 5/10/15/20/25 | `on_boss_defeated` |
| Сила Smart Weighting | `affinity` | драфт тегов/наград |
| **Trigger Cap** | `MAX_TRIGGER_DEPTH=5` | `CombatManager` |
| **Канал тега** (damage/shield/heal) | `channel` | ForgeRegistry (Развилка №1) |
| **Гипер-заряд** (рост grade) | `OVERCHARGE_STEP=1.2` | forge.py (§4-bis) |
| Пост-босс ковка (каденция) | — | runner / `on_boss_defeated` |
| Процент-конвенция | — | все ивенты (% не флат) |
| Тир награды по этажу (10.5) | `reward_level_for_floor` | драфт/магазин |
| Маска временного uid (10.4) | `{parent}_temp_{id}` | resolve_forge_record |

См. [[card-upgrade-ceiling-engine]], [[balance-curve-framework]],
[[scaling-audit]], [[wall-ceiling-taxonomy-plan]], [[boss-filter-ladder]].
