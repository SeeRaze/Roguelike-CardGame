# Настройки pytest. Запуск: просто `pytest` из корня проекта.
[pytest]
testpaths = tests
pythonpath = .
addopts = -q
markers =
    ui: тесты, которым нужен pygame (отрисовка экранов в headless-режиме)
