# ui/combat/layout.py
# Палитра и геометрия боевого экрана (1920×1080). Без pygame и логики.

# ── Палитра ──────────────────────────────────────────────────────────────────
_BG           = (12, 12, 22)
_PANEL_BG     = (22, 22, 40)
_PANEL_BORDER = (160, 160, 255)
_GOLD         = (255, 220, 60)
_WHITE        = (220, 220, 220)
_RED          = (220, 80, 80)
_GREEN        = (80, 210, 100)
_BLUE         = (100, 180, 255)
_GRAY         = (120, 120, 140)
_INTENT_OTHER = (180, 180, 255)

# ── Геометрия (1920×1080) ────────────────────────────────────────────────────
_SW, _SH      = 1920, 1080
_PANEL_W      = 560
_PANEL_MARGIN = 30          # отступ от края экрана
_PANEL_TOP    = 70          # верх панелей (под relic-баром)
_INNER_PAD    = 25          # отступ текста внутри панели

# Левая панель (игрок)
_P_PX  = _PANEL_MARGIN                          # 30
_P_IX  = _P_PX + _INNER_PAD                     # 55

# Правая панель (враг) -- зеркально
_E_PX  = _SW - _PANEL_MARGIN - _PANEL_W         # 1330
_E_IX  = _E_PX + _INNER_PAD                     # 1355
_E_IW  = _PANEL_W - _INNER_PAD * 2              # 510  (ширина контента)
